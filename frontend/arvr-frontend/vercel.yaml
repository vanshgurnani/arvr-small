version: 2

builds:
  - src: package.json
    use: "@vercel/next"
    config:
      outputDirectory: .next
      installCommand: npm install
      buildCommand: npm run build

routes:
  - src: /(.*)
    dest: /index.html

